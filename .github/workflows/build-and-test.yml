name: dependabot
on: push

jobs:
  echo-input:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20.x'
      # - run: npm ci
      # or one-off:
      - run: npm install libsodium-wrappers
      - uses: actions/github-script@v7
        env:
          OWNER: knkarthik
          REPO: action-test
          SECRET: supersecret
        with:
          github-token: ${{ secrets.DEP_PAT_TEST }}
          script: |
            const OWNER = process.env.OWNER
            const REPO = process.env.REPO
            const SECRET = process.env.SECRET
            const sodium = require('libsodium-wrappers');
            await sodium.ready;
            const data = await github.rest.dependabot.getRepoPublicKey({
              owner: OWNER,
              repo: REPO,
            });
            let encrypted = ''
              console.log("key printed")
              console.log(data.key)
              let key = data['key']
              let binkey = await sodium.from_base64(key, sodium.base64_variants.ORIGINAL)
              let binsec = await sodium.from_string(SECRET)

              let encBytes = await sodium.crypto_box_seal(binsec, binkey)
              let output = await sodium.to_base64(encBytes, sodium.base64_variants.ORIGINAL)

              console.log(output);
              return output;
              
              resp = await github.rest.dependabot.createOrUpdateRepoSecret({
              owner: OWNER,
              repo: REPO,
              secret_name: "DEPENDABOT_SECRET",
              encrypted_value: await generatesecret()
              });
            
